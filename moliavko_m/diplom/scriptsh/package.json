{
  "name": "your-project-name",
  "version": "1.0.0",
  "scripts": {
    "test": "newman run postman_collection.json --reporters cli,htmlextra --reporter-htmlextra-export ./report/report_$(date '+%Y_%m_%d_%H_%M_%S').html"
  },
  "dependencies": {
    "newman": "^5.2.4",
    "newman-reporter-htmlextra": "^1.3.0"
  }
}
